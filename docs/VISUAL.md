# Raseed — Visual Reference

Canonical tokens and component rules: `docs/design-handoff/`.

- Board: `docs/design-handoff/raseed-ops-board.html`
- Tokens + CSS: `docs/design-handoff/handoff/raseed.css` (copied to `src/styles/raseed.css`)
- Brand: `docs/design-handoff/brand-spec.md`

Do not invent hexes. Status uses the five StatusPill tones only. Money always renders via `<Money>` as `Rs 12,450`.
