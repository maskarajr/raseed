# Agent rules

- SoT: GitHub `maskarajr/raseed`. Origin `pixelmoose/raseed` is mirror only.
- Never use the old Origin-scoped agent token (deleted native repo).
- Default branch `main`. Vercel branch `demo`. Do not merge demo Turso bits onto `main` unless asked.
- Payments: COD orders; collect on invoice (part or full); never credit; no cash advance.
- Keep GitHub issue #6 and closed PRs #1–#5 as archive. Do not recreate Origin PRs that already sit in `main`.
- Full lock: `CONTEXT.md`.
